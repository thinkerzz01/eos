# Work Log - 2026-09-19

Branch: `main` (all pushed to `origin/main`). Everything below is built, type-checked (`npx tsc --noEmit`), production-built (`npx next build`), committed and pushed. All SQL migrations listed were RUN by the owner in the Supabase SQL editor on 2026-09-19.

Not runtime-verified in the browser for signed-in (admin/teacher/student) flows - the agent cannot log in. Public `/book` field render was confirmed. Owner still to eyeball the authed screens (see "Owner to verify" at the end).

---

## 1. Billing v2 - monthly vs upfront, auto-vouchers, forecast
Status: DONE. Detail + resume steps in `BILLING-PLAN.md`.

- Each student has a billing plan: `billing_mode` = `monthly` (per-student auto-voucher, cut ~5 days before their own `next_due_date`, until `billing_end_date`) or `upfront` (one paid block at enrollment, no monthly fees/reminders until the end). `billing_start_date` / `billing_end_date` drive the schedule; awkward mid-month dates handled by explicit start/end pickers. No proration (owner chose full-month). Upfront price is a package amount typed at convert.
- Engine `lib/cron/billing.ts` (wired into `/api/cron/tick` + `/api/cron/reminders`); `recordPayment` advances the cycle on full payment of the current voucher.
- Forecast: `AdminData.forecast` (recurring next month, billed-not-collected, upfront excluded) on the dashboard Fees card.
- Convert flow: `app/leads/actions.ts` (`convertLead`), modal in `app/leads/LeadsClient.tsx`.
- Migration: `supabase/migrations/2026-09-19_billing_modes.sql` (RUN).

## 2. Session capture on booking / lead / convert
Status: DONE.

- Exam-session picker (Oct/Nov 2026, May/Jun 2027, Oct/Nov 2027, May/Jun 2028, Custom...) via shared `lib/sessions.ts`.
- On the in-app Add-Lead modal and the public `/book` form; convert modal prefills the session from `leads.exam_session`.
- Migrations: `2026-09-19_booking_session.sql` (extends `create_public_booking` RPC to store the session) (RUN).

## 3. Teacher salary - exact class start/end dates
Status: DONE.

- Replaced the coarse "first paid month" with exact `class_start_date` + `class_end_date` on `student_subjects` (modal on Teacher Payouts).
- The 25% first-month commission falls in the start date's month. END DATE STOPS THE SALARY (owner confirmed): monthly view shows "Ended"/zero after the end month; the "All" view caps months at the end.
- Salary Period column now shows the real class span ("From 01 Sep 2026" / "01 Sep - 30 Nov 2026 · N mo") instead of a dateless "All · N mo".
- Files: `lib/data/teacherSalaries.ts`, `app/teacher-payouts/actions.ts` (`setEnrollmentSalary`), `app/teacher-payouts/TeacherPayoutsClient.tsx`.
- Migration: `2026-09-19_enrollment_class_dates.sql` (RUN; backfills start from `salary_start_month`).

## 4. Finance / Revenue tab
Status: DONE. New page `/finance` (admin, under Finance in the sidebar - "Revenue & Profit").

- P&L two ways: Accrual (fees billed - salaries earned - expenses = profit) and Cash (received - paid - expenses = net cash). Shows fees still to collect and salaries still to pay.
- Per-student margin (fee - teacher cost). Per-teacher earned vs paid with variance NOT floored (so overpayments show, e.g. a teacher paid more than earned reads "Overpaid Rs X").
- Expenses ledger (rent/ads/utilities/etc), add + delete, admin-only. Without expenses entered, the profit line is really "margin after salaries" (labeled honestly).
- Files: `lib/data/finance.ts`, `app/finance/{page,FinanceClient,actions}.tsx`, sidebar `components/layout/Sidebar.tsx`.
- Migration: `2026-09-19_expenses.sql` (RUN).
- OPEN: no other-expense categories are pre-seeded; "true profit" only once the owner enters expenses.

## 5. Communications switch - only demo + class comms
Status: DONE (owner-requested; reversible from one file).

- Single control: `lib/notifications/policy.ts`.
  - Emails allowed: `class_reminder`, `class_rescheduled`. Demo emails are DIRECT sends (booking confirmation + teacher assignment) so unaffected; the family's demo invite is a Google Calendar invite. Auth emails (password reset / invite) are essential and NOT governed here.
  - In-app allowed categories: `class`, `demo`.
- Suppressed now: fee_due, grace_ending, grace_expired_admin, payment_received, monthly_report, follow_up (emails) + homework in-app pings.
- Enforcement: `enqueueNotification` returns `suppressed` for disallowed types (never queued); `runSend` soft-deletes any already-queued disallowed rows; `notifyUserIds` drops in-app notices whose `category` isn't enabled.
- CRONS STAY ON. They still generate vouchers, send class reminders, and drain the queue. To re-enable a channel later, add its type/category to `policy.ts` (one line) - no cron change.

## 6. Earlier this session (context)
- 12-hour "4:00 PM" time format system-wide.
- Schedule: add-to-calendar button, choose-who-to-invite (single + bulk), new-class student alert.
- Syllabus module locked to admin + manager (see `SYLLABUS-PLAN.md`).

---

## Parked / decide later
- Billing: one-click "renew upfront student as monthly" when a block ends (currently a manual edit of billing_mode/dates).
- Finance: pre-seed common expense categories; consider "true profit vs margin-after-salaries" labeling once expenses are used.
- Comms: owner will say when to re-enable fee reminders / payment receipts / etc. Auth emails currently kept on - owner to confirm that stays.
- Older parked items: `FEATURE-BACKLOG.md`, `DESIGN-AUDIT.md`, syllabus content sign-off (`SYLLABUS-PLAN.md`), Zoom-vs-Meet class recording, go-live switch to portal.thinkerzz.com.

## Owner to verify (signed-in screens the agent could not test)
1. Convert a monthly and an upfront lead; check the voucher(s) + dashboard "Recurring next month".
2. Set a class start + end date on a salary row; confirm Sep/Oct/Nov figures and that it reads "Ended" after the end month.
3. Open Revenue & Profit; add a couple of expenses; confirm the profit/net-cash numbers.
4. Confirm only demo + class emails/notifications actually go out.
